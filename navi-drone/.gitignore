# Python
__pycache__/
*.pyc
venv/
*.egg-info/

# Node / Expo
node_modules/
.expo/
*.log

# Runtime data generated by the Pi server
pi_server/navi_drone_log.db

# Raw captured face photos - kept local only, not needed for deployment.
# Remove this line if you want to version-control the dataset itself.
core/dataset/

# NOTE: core/models/*.onnx and core/known_faces.json are intentionally
# NOT ignored - they need to travel with `git clone` to the Pi.
